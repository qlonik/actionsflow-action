name: Release

on:
  push:
    branches:
      - "main"
    paths-ignore:
      - "dist/**"
  workflow_dispatch:
  repository_dispatch:
    types: [new_version_with_actionsflow]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Checkout Branch
        run: git checkout main
      - name: Configure CI Git User
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com
      - uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node
      - name: Use Node.js LTS
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
      - name: Update & Build
        run: |
          npm ci
          npm update
          npm run build
          npm run test
      - name: Git status
        run: git status
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git add dist/main/index.js dist/post/index.js package.json package-lock.json
          git diff-index --quiet HEAD || git commit -m "chore(release): 🤖 update actionsflow dependence & build bundle"
          npm run release
